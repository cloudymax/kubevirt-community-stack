{{- range $reg, $props := .Values.users }}
{{- if eq $props.password.random "true" }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $props.name }}-password
  namespace: {{ $.Values.namespace }}
type: "Opaque"
data:
  password: {{ randAlphaNum 16 | b64enc }}
{{- end }}
{{- end }}

